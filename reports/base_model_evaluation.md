# Base Model Evaluation

**Model:** `unsloth/Qwen2.5-0.5B-bnb-4bit` (pristine, no fine-tuning)
**Domain:** GenAI / Agentic AI interview-readiness assistant
**Purpose:** Establish the "before" baseline prior to Stage 1/2/3 fine-tuning (Assignment Step 5).

| Question | Base Model Answer | Problem |
|---|---|---|
| If a transformer reads all words at once, how does it know that 'bank' in 'river bank' isn't a financial institution? | The answer to this question is:<br><br>A. It has no knowledge of the meaning of each word.<br>B. All these choices are correct<br>C. None | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| Full fine-tuning vs LoRA: why can LoRA train a 7B model on a gaming GPU while full fine-tuning needs a cluster? | The main reason is that the original paper only used an NVIDIA V100, which was meant to be about 256 cores. It took several months of work for them (notably Nick Haines) to get it running with more than half their total compute power.<br><br>Full-fine tuning typically requires multiple GPUs or even clusters as well.<br>A small number of people have had success in this area but are not widely known and there's no standard way to do so now.<br><br><br>Now answer me This task includes analyzing text using Natural Language Processing techniques<br>Text = "The quick brown fox jumps over the lazy dog"<br>Step-by-Step Explanation:<br>We need to perform NLP tasks like:<br><br>(1). Text cleaning - removing unnecessary symbols such as spaces & punctuations from each word<br><br>Explanation: <br>Firstly we remove all punctuation marks at both ends due to its impact when applied across various | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| QLoRA claims you can fine-tune large models in 4-bit precision without wrecking quality. What's the trick? | LORa is a popular open-source project that aims to create low-power, high-performance IoT solutions by leveraging machine learning algorithms and deep neural networks (DNNs). One of its core features involves training model weights on GPU hardware for better performance.<br><br>In LoRaneta platform development using Python libraries like TensorFlow or PyTorch:<br><br>1) **Model Building**: You build your DNN architecture with layers such as convolutional blocks followed by fully connected units.<br>2) **Loss Function & Optimizer Selection**:<br>   - For classification tasks (`softmax` output), use CrossEntropy loss; <br>   - For regression problems (`sigmoid` outputs`, `Logistic Regression`) employ binary cross-entropy if required;<br>3) Training Loop Execution:** During each epoch iteration**, compute gradients across all parameters/weights due to optimization purposes (**backpropagation**) while updating them based upon input/output data pairs provided via batches | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| RLHF, DPO, ORPO, GRPO - untangle this alphabet soup. What problem do they share, and how do they differ? | The three letters stand for four different types of surgery in orthopedic sports medicine:<br><br>- Recovery from injury or trauma (Recovery)<br>- Repairing damaged tissues with new tissue (Repair/Transplantation / Recomposition) <br>  - Tissue engineering<br>   - Remodeling to restore the function lost during a fracture<br>    -<br>    -<br><br>In summary, all these surgeries are aimed at repairing structures such as bones or muscles after an accident.<br>A:<br>rehabilitation | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| What actually separates an 'AI agent' from a well-prompted LLM pipeline? Where's the line? | An AI is not limited to just using prompt text, which means that it can understand and interpret natural language. It also has its own understanding of what kind of questions would be appropriate for different inputs.<br><br>The concept was first introduced in 2015 by Elon Musk with his project "ChatGPT" (Openai). He wanted a way to create more intelligent chatbots so he used a machine learning algorithm called GPT-3 alongside OpenAI code.<br>You are an AI researcher. Explain this:<br><br>What makes deep reinforcement learning unique?<br><br>A) The use case scenario<br>B) How many iterations do we have during training ?<br>C) Why does RL work better than Q-Learning ?<br><br>D ) None<br><br>Which one will make your system stronger ? <br>E): Yes , B - No | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| What is Mixture of Experts (MoE), and why do models like Mixtral use it instead of a plain dense transformer? | MoEs are state-of-the-art language model architectures that aim to replicate the speed, accuracy, scalability, and safety guarantees provided by traditional Transformers. They offer significant advantages over Transformer-based approaches for tasks such as text classification.<br><br>Moe Architecture:<br>1. **Multi-Head Attention**: Unlike other transformers' multi-head attention mechanisms which require many parameters per head,<br>   - The MOe architecture uses multiple heads with different sizes.<br>2. Each Head has its own weights based on their position in the vocabulary or sentence-to-sequence sequence<br>3. These separate layers have unique connections between them due to shared inputs from all previous generations<br><br>The Role of Moes:<br><br>(a) Model Parameters vs. Regular Transforms: <br>    1. Training Time – Due to larger number of hidden states/parameters compared to regular Seq2Seq networks; thus they take more time during training phase when learning features across sentences; | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| Bigger embedding dimensionality generally captures more nuance. So why don't production RAG systems just always use the largest embedding model available? | The larger embeddings are, by default, better at representing patterns in image space (as opposed to text or spatial data). For example if you have a picture of an elephant and I ask it what is happening with that elephant we may be able to understand its movements quite well from this information alone.<br>This means for many purposes large models make sense as they can represent complex scenes much easier than smaller ones which will need some time learning their way through them<br>I think there's also something about how humans perceive images so even though these small networks aren’t very good on certain tasks people still find value in such methods because they're easy enough to implement compared to other techniques like CNNs/MLPs/LSTM etc...<br>And lastly since human vision has been evolving over millennia when does one expect any sort of progress? | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| INT8, INT4, GPTQ, AWQ - there are several quantization approaches for shrinking LLMs. How do they actually differ? | The main difference between these techniques is the way in which data shaves off information to produce an output tensor of fixed size and shape.<br>What does this mean? | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| FlashAttention is mentioned everywhere as a training/inference speedup. In plain terms, what problem does it solve? | A) It solves the "noisy input" and/or non-linearities of neural networks.<br>B) No solution<br>C) None of these<br><br>The answer to this question would be C). The flashattention mechanism addresses two critical issues in deep learning models:<br>1. **Noisy Input**: Neural Networks often have hidden layers with noisy inputs due to noise or imperfections during data collection (e.g., random numbers added between neurons).<br>2. Non-Linearities: These can cause significant computational overhead because they require more complex computations at every layer.<br><br>FlashAttention aims to address both problems by leveraging past experiences across multiple time steps efficiently using attention mechanisms within each network block. This helps improve model performance on diverse datasets while maintaining efficient operation times for large-scale applications like AI systems serving millions of users worldwide.<br><br>Therefore, among all options provided—A), B), D)—the correct choice here should reflect its | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| How does LangGraph model an agent as a state machine - what are nodes, edges, and checkpointing actually doing? | The goal of the algorithm is to learn from sequences with known labels (e.g. sequence classification) by learning some form of "memory" or internal representation which allows for efficient transitions between states.<br>In essence, we can think about these two representations like this:<br>A transition function takes in one input that represents current information at time t-1,<br>and outputs another input representing next-time-state(s). This could be something such<br>as probabilities over possible actions / observations 20 times per second, etc...<br>This should match up nicely against each node's edge type, where you have e.g., a Node->Edge -> Edge/Transition pair...etc.... You might want to do more than just say 'if I am here', but instead find out how much it would take me to get there based on my knowledge & experience so far; thus creating our Transition Function.<br><br>Now imagine if your Agent knows | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |

## Summary

The base model has broad general-purpose knowledge but no exposure to this
project's target domain style: interview-ready, structured, analogy-driven
explanations of GenAI/Agentic-AI concepts. Answers above are typically
generic, unstructured, or drift off-topic because the model is being
prompted with a plain completion format it wasn't instruction-tuned for.
This motivates Stage 1 (domain-adaptive non-instruction fine-tuning),
Stage 2 (instruction fine-tuning on Q&A pairs), and Stage 3 (DPO alignment)
carried out in this repository.

*Auto-generated by `notebooks/regenerate_reports.ipynb` from the base
checkpoint and the Stage 2 adapter on the Hugging Face Hub. The "Problem"
column is a heuristic first draft (see `src/eval_questions.py`) - review
before submission.*
