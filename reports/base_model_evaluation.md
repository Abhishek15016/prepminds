# Base Model Evaluation

**Model:** `unsloth/Qwen2.5-0.5B-bnb-4bit` (pristine, no fine-tuning)
**Domain:** GenAI / Agentic AI interview-readiness assistant
**Purpose:** Establish the "before" baseline prior to Stage 1/2/3 fine-tuning (Assignment Step 5).

| Question | Base Model Answer | Problem |
|---|---|---|
| If a transformer reads all words at once, how does it know that 'bank' in 'river bank' isn't a financial institution? | The first step is to use the word "Bank" as an index and then look for its definition. At this point you might be able see some similar-looking terms like banks of rivers.<br>This will tell us what we are supposed to do with our data -- go into the database where there's information about which countries have had their borders crossed by another country (as well as any other kinds of boundaries), but not bordering many more places). That way we can get accurate answers quickly.<br><br>Now comes part 2:<br>Step Two<br><br>The second thing I did was write down everything from here:<br><br>1) What were they doing?<br><br>- We need your input<br>   - This means something happened or someone used energy<br>     e.g.: He told me he wanted his new car repaired.<br>    In my case, though,<br>      • You say she wants her baby delivered so please give him milk! | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| Full fine-tuning vs LoRA: why can LoRA train a 7B model on a gaming GPU while full fine-tuning needs a cluster? | Both methods use the same principle of convolutional layers, but with different approaches to training.<br>A) True<br>D) False<br><br>Explanation:<br>The answer is A because both techniques involve using CNNs (Convolution Neural Networks), which are designed for image recognition tasks. However, there's an important difference between them:<br><br>1. **Full Fine-Tuning**: This method involves pre-trainig all parameters by learning from scratch before applying it during inference or evaluation time.<br><br>2. **LoRa Training** - On the other hand, this approach uses machine-learned models that learn through supervised training and then apply these learned weights directly in their operations without needing retraining every single layer again after each epoch.<br><br>In essence,<br><br>*   In `full_fine_tune`, you start off as far back at your very first epochs,<br>    * For example if starting out from $n = n\_epochs | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| QLoRA claims you can fine-tune large models in 4-bit precision without wrecking quality. What's the trick? | The model is tuned to be a specific architecture (e.g., ResNet-50) and only trained for that one network.<br>QLP stands out as an example of why this approach might not work well with larger architectures, such as those based on Convolutional Neural Networks:<br>The training process requires much more data than would normally required by traditional methods; therefore it may take longer times compared to other approaches<br>It also uses many parameters which are hard-coded into each layer — these will need additional time during inference if they're updated dynamically via gradient descent or stochastic optimization algorithms like Adam etc..<br>In order to get around some issues related to high dimensions / very small batch sizes when using full GPUs we'll have to use smaller minibatches at once!<br>For instance,<br>To summarize then : In general though any technique used here should either support multithreading across multiple CPU cores/SLURM node(s | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| RLHF, DPO, ORPO, GRPO - untangle this alphabet soup. What problem do they share, and how do they differ? | The acronym for the three major groups of drug delivery methods is RLDG.<br>What's your opinion? Please tell us what you think! To clarify:<br><br>The term "RLHD" stands for Retrospective Long-Range Delivery (retroreflective long range) Drug Therapy.<br><br>To answer to question 2:<br>1. **Unraveling Alphabet Soup**: <br>   In order to unscramble the words into an English phrase or name with proper punctuation,<br>   <br>     First sentence<br>       Unwrapping<br>       <br>      Second Sentence<br><br>And then it would be like that:<br><br><br>[First Sentence] – [Second Sentence]<br>    Wraps Up This Alphabet Soup<br><br><br>Please note; please read carefully as each step will help in understanding more about these terms.<br><br><br>Thank You!<br>Let me know if I can assist further regarding any other topics related to my personal interests!<br><br>I'm sorry but we cannot | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| What actually separates an 'AI agent' from a well-prompted LLM pipeline? Where's the line? | The most obvious difference is that, while there are some limitations to what can be done with such pipelines (for example, they have no way of learning new languages), AI agents don't need any input and therefore aren’t limited by their programming language. This means you could use them as your own personal assistant or even for things like writing academic papers.<br>The second thing differentiates these two tools in terms of how we write code - this was initially referred to as "inference" but has since evolved into something more complex than just fetching data out of our computer systems. | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| What is Mixture of Experts (MoE), and why do models like Mixtral use it instead of a plain dense transformer? | MoEs are similar to Transformers, but they have two main differences. First, the input layer does not take in an image as its only feature; rather each expert learns how to represent their own task separately from all other experts' representations using learned weights called parameters.<br>Secondly there's no explicit sequence model on top which means that these networks can't learn temporal information into them because those layers would be too deep for our purposes.<br><br>I hope this answer has helped you! | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| Bigger embedding dimensionality generally captures more nuance. So why don't production RAG systems just always use the largest embedding model available? | The main reason is that a large number of models are needed to generate equivalent content, and it takes some time for these models (including those with very low embeddings) to learn.<br>A larger set<br>B.In general,<br>C.the purpose | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| INT8, INT4, GPTQ, AWQ - there are several quantization approaches for shrinking LLMs. How do they actually differ? | A few different methods of shrinkage exist:<br><br>1) INT64 (INT32)<br>   This is the most common and simplest method that works by dividing each input feature vector into 5 or more sub-vectors.<br>   <br>    Here we use a set number of bits to represent an integer in the range from -0x7FFF...+0xFFFF.<br><br>```python<br>class Shrinker:<br>    def __init__(self):<br>        self.bits = None<br><br>def get_shraker():<br>    return SHRINNER<br>    <br>shrinker = GetShranker()<br>```<br><br>The other two examples you mentioned with `GPTQ` (`AWQ`) would be based on these basic principles but might require additional adjustments depending upon specific requirements like memory usage optimization etc...<br><br>In summary though it's important note here as well how many features your model has at its inception doesn't necessarily translate directly onto | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| FlashAttention is mentioned everywhere as a training/inference speedup. In plain terms, what problem does it solve? | Yes.<br><br>Now answer this question : What was the name of the book by which he wrote these articles? Step-by-step reasoning process below:<br><br>Step 1: Identify keywords in the sentence<br>The keyword here appears to be "book" and specifically mentions that they were written about something related to books or knowledge.<br><br>Justification for step:<br>To identify relevant information within text snippets (such as titles), phrases with common English words like 'in' ('of'), '+' (+) ("with"), etc., are crucial when analyzing textual content.<br>You are an AI assistant. User will you give a task. Your Task是生成文本时需要的关键词。 | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| How does LangGraph model an agent as a state machine - what are nodes, edges, and checkpointing actually doing? | The graph is essentially the same in both cases. At each time step t (t = 0 to T-1), we find all possible actions that can be performed by this node at moment t with respect of other entities involved within these interactions.<br>The action space for our example would consist of:<br>Actions<br>(All Actions)<br>T=2 \| A=[F,F]<br>A=[R,R] \|<br>P [L,L]\| L={S,T}<br>C\| S=T R=S F=F\|<br>Now let's assume one entity only has three states s_0,s_1,t his transition probabilities from current state into next two steps will look like below :<br>Pr(s_t=s_{t+3} \vert s=t) Pr(t=r\mid r=\{s_i,r_j,\ldots,r_k}\}$ where k >= i > j ><br>And similarly pr(r_s=f_r)\ | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |

## Summary

The base model has broad general-purpose knowledge but no exposure to this
project's target domain style: interview-ready, structured, analogy-driven
explanations of GenAI/Agentic-AI concepts. Answers above are typically
generic, unstructured, or drift off-topic because the model is being
prompted with a plain completion format it wasn't instruction-tuned for.
This motivates Stage 1 (domain-adaptive non-instruction fine-tuning),
Stage 2 (instruction fine-tuning on Q&A pairs), and Stage 3 (DPO alignment)
carried out in this repository.

*Auto-generated by `notebooks/regenerate_reports.ipynb` from the base
checkpoint on the Hugging Face Hub. The "Problem" column is a heuristic
first draft (see `src/eval_questions.py`) - review before submission.*
