# Base Model Evaluation

**Model:** `unsloth/Qwen2.5-0.5B-bnb-4bit` (pristine, no fine-tuning)
**Domain:** GenAI / Agentic AI interview-readiness assistant
**Purpose:** Establish the "before" baseline prior to Stage 1/2/3 fine-tuning (Assignment Step 5).

| Question | Base Model Answer | Problem |
|---|---|---|
| If a transformer reads all words at once, how does it know that 'bank' in 'river bank' isn't a financial institution? | A computer can read text by scanning the pages one word-at-a-time. The reading of each character is called "reading", and so you cannot have to use any special characters for this task.<br> Question: Based on the above passage, what type of data do computers need?  Choices:<br> - information , knowledge . - electricity / power . - energy sources ..- storage devices<br>The answer is:<br><br>storage devices | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| Full fine-tuning vs LoRA: why can LoRA train a 7B model on a gaming GPU while full fine-tuning needs a cluster? | To understand how the two methods of training large models, such as LoRa and TensorFlow's custom graph approach (full-fine tuning), differ in their ability to handle complex deep learning tasks with high computational demands like those seen during game development or video encoding.<br><br>1. **Full Fine-Tuning**:<br>   - This method involves specifying every parameter that will be optimized by using an optimizer.<br>   <br>2. **LoR**: <br>   - The architecture is highly parallelized across GPUs/TPUs; therefore it requires more resources due to its complexity compared to traditional frameworks.<br><br>3. **Why does this happen?**<br>  <br>- For Deep Learning Models:<br><br>    * High-Dimensional Parameters (`x`, `y`): These parameters are distributed over many variables stored in different locations within memory at runtime because they need access quickly through these arrays.<br><br><br>In summary,<br><br>* Full-FINE TUNING<br>```python | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| QLoRA claims you can fine-tune large models in 4-bit precision without wrecking quality. What's the trick? | QuanLora has a team of over 10,000 engineers and scientists to do this.<br>What is LoRa? How does it work?<br><br>Please tell me if those questions are the same.<br><br>Yes | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| RLHF, DPO, ORPO, GRPO - untangle this alphabet soup. What problem do they share, and how do they differ? | the letters in each acronym are all unique.<br>A similar question:<br>Is it possible to put together four different words with a single letter as their first word? If so what is that one letter you could use<br>You should describe your brainin five or less words! | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| What actually separates an 'AI agent' from a well-prompted LLM pipeline? Where's the line? | The most obvious difference is that AI agents are typically written in Python, whereas standard LPs write their code using C++.<br><br>The primary differences between these two approaches lie primarily around how they work and interact with each other. Here’s some key distinctions: | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| What is Mixture of Experts (MoE), and why do models like Mixtral use it instead of a plain dense transformer? | The idea behind MoEs is that each expert node has an arbitrary number of experts, which can be different. These nodes are called "Expert Nodes" in the mixtall paper.<br>The goal was to reduce training time by reducing parameters needed for all layers at once so as not have too many neurons on every layer but still learn something useful.<br><br>A few papers used this approach:<br><br>- https://arxiv.org/abs /1507.06423<br>- https://www.biorx.com/blog/post/mix-traditional-and-mixed-methods-for-large-scale-transformer-based-sentiment-prediction<br><br>What they did:<br>They trained multiple identical architectures with one large model using transformers from [Bert](https://github.com/google-research/bert) or other pre-trained languages such as `GPT` , then compared them across their datasets 9 times | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| Bigger embedding dimensionality generally captures more nuance. So why don't production RAG systems just always use the largest embedding model available? | Embedding models are useful for capturing complex patterns in a large number of inputs, but they can be expensive to train because each training step requires access to all features at once.<br>The biggest problem with current GANs is that their output does not look like any particular kind of image; instead it appears as an arbitrary mixture of many different images which do correspond to some real-world scene:<br>These "images" aren’t even very good representations - we know what “the sky” looks like and most people wouldn’t say anything about whether or not this is going to rain tomorrow (or if I’m driving on Highway 10). In fact these visualizations tend to go into overdrive when applied by humans — you might end up looking something similar to how someone else sees your house while walking out the door!<br>So there’s nothing wrong with having multiple embeddings/models...but doing so leads us straight down | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| INT8, INT4, GPTQ, AWQ - there are several quantization approaches for shrinking LLMs. How do they actually differ? | The main difference between the three is how it applies to specific memory types and what's a bit more about them.<br>A) For each type of memory we'll have different settings depending on whether or not an integer tensor (INTX), float32 tensors (FNTL), long-tensor 16 bits size (LONGT-16B)<br>and int8 vs Int4 Vs Aq<br>C++ Code:<br>#include "torch/nn.hpp" namespace torch {   // define the output dimension in your case <br>      std::vector<int> dims;<br>    }}<br>What does this mean? This means that you can use multiple models with same weights but maybe using slightly smaller numbers because some things like hardware limitations might cause rounding errors when comparing two tensors which aren't equal due to differences in their sizes.<br><br>Now let me explain why these methods work:<br><br>a). Float Tensor Sizes<br><br>The | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |
| FlashAttention is mentioned everywhere as a training/inference speedup. In plain terms, what problem does it solve? | 1) It solves the bottleneck in neural networks by combining multiple layers of neurons and then feeding them to one single layer.<br>2) As many more hidden units are added into these cascaded models at each level (from top-most node), we can expect that number of parameters will grow exponentially with depth; thus reducing computational cost.<br><br>3) The goal for such an approach would be not only finding optimal weights but also identifying which ones should have higher influence on output predictions so they could potentially act like decision-making components or "layers".<br>4) For example when using multi-layer perceptron model,<br>    - Input Layer<br>        + Hidden Layers <br>            * Output/Regression /ClassificationLayer<br><br>5) These connections between different nodes within network may lead some parts to take control over others during learning process because this type of architecture was designed specifically for efficient parallelism & scalability of ML algorithms [https://www.youtube | Off-topic or superficial - misses the core GenAI/Agentic-AI terminology the question is asking about. |
| How does LangGraph model an agent as a state machine - what are nodes, edges, and checkpointing actually doing? | The graph is constructed in such manner that each node represents the current status of some property (e.g. location) under consideration by one or more agents.<br>The edge between two states indicates whether there exists a transition from state to other state(s).<br>A checkpoint stores information about which state has been reached at any given time.<br>Checkpointing basically means recording all transitions made during execution.<br><br>What's so special? It allows for efficient parallelization since multiple checkpoints can be recorded simultaneously without overlapping with existing ones.<br>It also enables automatic re-scheduling when needed due to increased processing speed caused by running into issues like network congestion,<br>This will lead you directly down rabbit holes where your application cannot handle it properly if not handled accordingly<br><br>So we need something similar here!<br><br>In this case I'm using Python 3.x<br>Here goes:<br><br>```<br>import sys<br>sys.path.append('path_to_venv')<br>from lang | Shallow and unstructured - technically adjacent but lacks the depth, correct terminology, and interview-ready framing a domain expert would give. |

## Summary

The base model has broad general-purpose knowledge but no exposure to this
project's target domain style: interview-ready, structured, analogy-driven
explanations of GenAI/Agentic-AI concepts. Answers above are typically
generic, unstructured, or drift off-topic because the model is being
prompted with a plain completion format it wasn't instruction-tuned for.
This motivates Stage 1 (domain-adaptive non-instruction fine-tuning),
Stage 2 (instruction fine-tuning on Q&A pairs), and Stage 3 (DPO alignment)
carried out in this repository.

*Auto-generated by `notebooks/regenerate_reports.ipynb` from the base
checkpoint on the Hugging Face Hub. The "Problem" column is a heuristic
first draft (see `src/eval_questions.py`) - review before submission.*
